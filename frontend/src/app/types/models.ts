export interface Organization {
    id: number;
    name: string;
    abbreviation: string;
}

export interface Tournament {
    id: number;
    name: string;
    year: number;
}

export interface Activity {
    id: number;
    name: string;
}